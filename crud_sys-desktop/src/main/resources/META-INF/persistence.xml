<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
	version="2.1">
	<persistence-unit name="appUnit" transaction-type="RESOURCE_LOCAL">
		<!-- provider>org.hibernate.ejb.HibernatePersistence</provider> <class>br.com.cams7.app.model.Mercadoria</class -->
		<properties>
			<!--property name="hibernate.connection.driver_class" value="${datasource.driverClassName}" /> 
			<property name="hibernate.connection.url" value="${datasource.connection.url}" /> 
			<property name="hibernate.connection.username" value="${datasource.user-name}" /> 
			<property name="hibernate.connection.password" value="${datasource.password}" / -->
			<property name="hibernate.dialect" value="${hibernate.dialect}" />

			<property name="hibernate.show_sql" value="${hibernate.show_sql}" />
			<property name="hibernate.format_sql" value="${hibernate.format_sql}" />

			<property name="hibernate.hbm2ddl.auto" value="${hibernate.hbm2ddl.auto}" />

			<!-- property name="hibernate.cache.provider_class" value="org.hibernate.cache.NoCacheProvider"/ -->
		</properties>
	</persistence-unit>
</persistence>